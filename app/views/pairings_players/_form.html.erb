<%= form_for(@pairings_player) do |f| %>
  <% if @pairings_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pairings_player.errors.count, "error") %> prohibited this pairings_player from being saved:</h2>

      <ul>
      <% @pairings_player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pairing_id %><br>
    <%= f.text_field :pairing_id %>
  </div>
  <div class="field">
    <%= f.label :player_id %><br>
    <%= f.text_field :player_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
