== README

This is a concept of the binding side of the messaging backend for the Shift24 app

To test, create 2 users, bind them, sign in as each of them and go to /play, then send messages across the websockets

TODO: Auto-binding engine


* Ruby 2.1
